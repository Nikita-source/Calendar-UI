version: '3.3'

services:

  react:
    restart: always
    build: .
    container_name: react
    ports:
      - "3000:3000"
    networks:
      - app-network

networks:
  app-network:
    external: true